#include<bits/stdc++.h>
using namespace std;
using ll=long long;
#define pii pair<int,int>
#define x first 
#define y second
struct g{
    int x,y,z;
};
bool cmp(g a,g b){
    return a.x<b.x;
}
int main(){
    int n;cin>>n;
    vector<ll>a(n);
    for(int i=0;i<n;i++)cin>>a[i];
    map<int,int>mp;
    
}